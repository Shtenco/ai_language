name: NEXUS R5.14 Deep-Diverse Russian
on:
  push:
    branches: ["nexus-r514-deep-diverse"]
  workflow_dispatch:

jobs:
  train:
    strategy:
      fail-fast: false
      matrix:
        steps: [16384, 32768]
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install sentencepiece datasets
          python - <<'PY' || python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
          import torch
          print('torch', torch.__version__)
          PY

      - name: Download fixed Russian UD corpora
        run: |
          mkdir -p data/r514
          curl -fL https://raw.githubusercontent.com/UniversalDependencies/UD_Russian-SynTagRus/master/ru_syntagrus-ud-train-a.conllu -o data/r514/ru_synt.train.a.conllu
          curl -fL https://raw.githubusercontent.com/UniversalDependencies/UD_Russian-SynTagRus/master/ru_syntagrus-ud-train-b.conllu -o data/r514/ru_synt.train.b.conllu
          cat data/r514/ru_synt.train.a.conllu data/r514/ru_synt.train.b.conllu > data/r514/ru_synt.train.conllu
          curl -fL https://raw.githubusercontent.com/UniversalDependencies/UD_Russian-SynTagRus/master/ru_syntagrus-ud-test.conllu -o data/r514/ru_synt.test.conllu
          curl -fL https://raw.githubusercontent.com/UniversalDependencies/UD_Russian-GSD/master/ru_gsd-ud-test.conllu -o data/r514/ru_gsd.test.conllu
          sha256sum data/r514/*.conllu > data/r514/UD_SHA256SUMS.txt

      - name: Extract held-out UD text
        run: |
          python - <<'PY'
          from pathlib import Path
          def convert(src, dst):
              out=[]
              for line in Path(src).read_text(encoding='utf-8').splitlines():
                  if line.startswith('# text = '):
                      t=line[9:].strip()
                      if t: out.append(t)
              Path(dst).write_text('\n'.join(out)+'\n', encoding='utf-8')
          convert('data/r514/ru_synt.test.conllu','data/r514/ru_synt_test.txt')
          convert('data/r514/ru_gsd.test.conllu','data/r514/ru_gsd_test.txt')
          PY

      - name: Build UTF-8-safe diverse Russian corpus and disjoint graph memory
        env:
          HF_HUB_DISABLE_TELEMETRY: '1'
        run: |
          python benchmarks/prepare_r57_russian_corpus.py \
            --synt-train data/r514/ru_synt.train.conllu \
            --out data/r514/rich \
            --wiki-train-mb 20 \
            --wiki-memory-mb 5 \
            --wiki-test-mb 3 \
            --heritage-train-mb 6 \
            --heritage-test-mb 2 \
            --synt-train-mb 4 \
            --max-doc-kb 24
          cat data/r514/rich/CORPUS_META.json

      - name: Deep train ${{ matrix.steps }} steps
        run: |
          OUT="nexus_r514_${{ matrix.steps }}"
          TESTS=(
            data/r514/ru_synt_test.txt
            data/r514/ru_gsd_test.txt
            data/r514/rich/ru_wiki_test.txt
            data/r514/rich/ru_heritage_test.txt
          )
          python benchmarks/r514_deep_diverse.py \
            --train-text data/r514/rich/ru_train.txt \
            --train-docs data/r514/rich/ru_train_docs.jsonl \
            --memory-docs data/r514/rich/graph_memory_docs.jsonl \
            --tests "${TESTS[@]}" \
            --steps ${{ matrix.steps }} \
            --out "$OUT" \
            --threads 2
          cp data/r514/rich/CORPUS_META.json "$OUT/"
          cp data/r514/UD_SHA256SUMS.txt "$OUT/"

      - name: Print raw generations
        if: always()
        run: |
          OUT="nexus_r514_${{ matrix.steps }}"
          if [ -f "$OUT/01_RAW_GENERATIONS.txt" ]; then sed -n '1,320p' "$OUT/01_RAW_GENERATIONS.txt"; fi

      - name: Archive
        if: always()
        run: |
          OUT="nexus_r514_${{ matrix.steps }}"
          if [ -d "$OUT" ]; then
            tar -czf "NEXUS_R514_${{ matrix.steps }}.tar.gz" "$OUT"
            sha256sum "NEXUS_R514_${{ matrix.steps }}.tar.gz" | tee "NEXUS_R514_${{ matrix.steps }}_SHA256.txt"
          fi

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: NEXUS_R514_${{ matrix.steps }}
          path: |
            nexus_r514_${{ matrix.steps }}/
            NEXUS_R514_${{ matrix.steps }}.tar.gz
            NEXUS_R514_${{ matrix.steps }}_SHA256.txt
          if-no-files-found: warn
          retention-days: 30
